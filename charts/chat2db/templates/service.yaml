apiVersion: v1
kind: Service
metadata:
  name: {{ include "chat2db.fullname" . }}
  labels:
    {{- include "chat2db.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "chat2db.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
