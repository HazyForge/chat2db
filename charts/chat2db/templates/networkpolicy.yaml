{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "chat2db.fullname" . }}-deny-ingress
  labels:
    {{- include "chat2db.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "chat2db.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
  ingress: []
{{- end }}
